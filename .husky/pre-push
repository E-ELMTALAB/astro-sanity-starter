#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🚀 Running pre-push checks..."

# Run full Astro pipeline (includes build and render checks)
pnpm ci:astro

echo "✅ Pre-push checks passed!"
